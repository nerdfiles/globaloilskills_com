<?php




/*
 *
    @namespace add_action

    add_action('init', 'process_post');
        function process_post(){
        if(isset($_POST['unique_hidden_field'])) {
            // Determine state of monad nonce. Like on that wine site with the
            // Social Media driven stock photos, etc. State is correlated with
            // RequireJS's dependency chain; build to whatever backend you
            // wish, flip http://angular-ui.github.io/ui-router/sample/#/contacts/42
        }
    }
 *
 */
